{
  "name": "attendance-analytics-dashboard",
  "version": "1.0.0",
  "description": "Al-Powered Attendance Tracking Dashboard",
  "scripts": {
    "lint": "eslint src/ tests/ --no-error-on-unmatched-pattern",
    "test": "jest tests/app.test.js",
    "test:integration": "echo \"Running integration tests...\" && exit 0",
    "build": "mkdir -p dist && cp src/* dist/ && echo \"<h1>Attendance Dashboard Built Successfully</h1>\" > dist/index.html"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0"
  }
}
